import {Required, MinLength, MaxLength} from "@tsed/common";
import {Model, Entity} from "@tsed/objection";

@Entity("users")
export class User extends Model {
  @Required()
  @MinLength(1)
  @MaxLength(25)
  name: string;
}